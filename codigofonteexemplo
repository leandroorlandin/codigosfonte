#set($responseHeaders = $input.params('response-headers'))

#foreach($headerKey in $responseHeaders.keySet())
    #set($headerValue = $responseHeaders.get($headerKey))
    $context.responseOverride.header.put($headerKey, $headerValue)
#end

$input.body

