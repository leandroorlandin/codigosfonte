#set($headers = $input.headers)

#foreach($headerKey in $input.params('response-headers').keySet())
    #set($headerValue = $input.params('response-headers').get($headerKey))
    $context.responseOverride.header.$headerKey = $headerValue
#end

$input.body
